name: shared
description: A Very Good Project created by Very Good CLI.
version: 0.1.0+1
publish_to: none

environment:
  sdk: "^3.3.0"

dependencies:
  env:
    path: ../env
  flutter:
    sdk: flutter
  intl: ^0.19.0
  logger: ^2.5.0
  powersync: ^1.4.2
  ui:
    path: ../ui

dev_dependencies:
  mocktail: ^1.0.3
  test: ^1.25.2
  very_good_analysis: ^5.1.0
